import { colors } from "./colors";
import { space, lineHeights } from "./spacing";
import { fonts, fontSizes } from "./fonts";

export const theme = {
  colors,
  space,
  lineHeights,
  fonts,
  fontSizes,
};